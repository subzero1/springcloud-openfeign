# 端口
server:
  port: 8771
spring:
  application:
    name: eureka-client1
# Eureka配置
eureka:
  instance:
    # eureka服务端的实例名称
    hostname: client
  client:
     service-url:
      defaultZone: http://localhost:8761/eureka